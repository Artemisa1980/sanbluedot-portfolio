node_modules/
dist/
.superpowers/
.env
.env.local
.DS_Store
*.log
.firebase/
screenshots/

# Internal Sandy + Clodi team files — never publish to the public repo
CLAUDE.md
core.md
conductor.md
work-orchestra.md
brutal-critic-(bc).md
relay-log.md
.agents/
docs/

# Brand SOURCES stay on disk, never committed. Site-displayed assets in public/ ARE committed —
# they ship to the public web anyway (decision 2026-07-15). Keep public/ to site-referenced files only.
brand-assets/

# Local agent config. launch.json is site tooling and stays tracked; the local
# settings file holds machine paths and tool grants and must never be published.
.claude/settings.local.json
.maintenance-backups/
